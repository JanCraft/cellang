pair =
    {:( f, s )
        {:( which )
            if( equals( which, "f" ),
                {f;},
                {s;}
            );
        };
    };

pair_first = {:(p) p("f");};

pair_second = {:(p) p("s");};